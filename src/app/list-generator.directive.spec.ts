import { ListGeneratorDirective } from './list-generator.directive';

describe('ListGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new ListGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
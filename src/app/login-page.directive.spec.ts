import { LoginPageDirective } from './login-page.directive';

describe('LoginPageDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginPageDirective();
    expect(directive).toBeTruthy();
  });
});
